const api = "https://webapihernotes.azurewebsites.net/Api";

/// When true, every HTTP call is served from the in-app mock store
/// instead of the Azure backend. Keep this enabled for the demo deploy.
const bool useMocks = true;
